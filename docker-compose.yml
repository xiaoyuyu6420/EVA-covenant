services:
  eva-covenant:
    build: .
    ports:
      - "${PORT:-3004}:3004"
    environment:
      - ADMIN_PASSWORD=${ADMIN_PASSWORD:-admin123}
      - ADMIN_SECRET=${ADMIN_SECRET:-eva-covenant-secret}
    volumes:
      - eva-data:/app/prisma
    restart: unless-stopped

volumes:
  eva-data:
